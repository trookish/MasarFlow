fastapi>=0.110
uvicorn[standard]>=0.29
sentence-transformers>=3.0
chromadb>=0.5
pydantic>=2.6
httpx>=0.27

# Phase 2 — Intelligence tier (core). Heavy optional models (whisper/torch,
# OCR, CLIP) live in requirements/extra.txt and are installed on demand.
networkx>=3.2
# AST code analysis (code_analysis.py). tree-sitter-language-pack is the
# maintained successor of tree-sitter-languages (which has no wheels for
# Python 3.12+); it ships abi3 wheels for 3.10–3.14 and downloads grammar
# binaries on first use, cached afterwards. The module is lazily imported and
# degrades gracefully, so a failed install never blocks the service.
tree-sitter>=0.23
tree-sitter-language-pack>=1.0
trafilatura>=1.12
pypdf>=4.8
python-docx>=1.1
openpyxl>=3.1
