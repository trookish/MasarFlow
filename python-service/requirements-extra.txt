# Heavy optional models for file/doc intelligence (#7). Install on demand via
# `pip install -r requirements-extra.txt` and enable the matching parser in
# Settings. Kept out of the core requirements so a fresh venv stays small.
# Note: torch is large (~2GB) and pulled in by openai-whisper.

openai-whisper>=20231117
torch>=2.2
pytesseract>=0.3.10
Pillow>=10.0
transformers[vision]>=4.40
