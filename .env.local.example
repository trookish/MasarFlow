# Base URL of the local Python AI service (python-service/). Optional — the
# app works without it (search falls back to Fuse, chat context falls back to
# the full workspace snapshot). See python-service/README.md to run it.
PYTHON_SERVICE_URL=http://127.0.0.1:8000
